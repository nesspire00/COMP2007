@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model IEnumerable<sept14_2.Models.Genre>

<h2>Welcome to the Music Store</h2>
<ul>
    @foreach(var g in Model)
    {
        <li>@Html.ActionLink(g.Name, "Browse", "Store", new { genre = g.Name }, null)</li>
    }
</ul>